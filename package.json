{
  "name": "rede-lua-educacao-pages",
  "version": "6.0.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "frontend"
  ],
  "scripts": {
    "dev": "npm run build && wrangler pages dev frontend/dist --compatibility-date=2026-09-15",
    "build": "npm --workspace frontend run build",
    "typecheck": "npm --workspace frontend run typecheck && tsc -p tsconfig.functions.json --noEmit",
    "deploy": "npm run build && wrangler pages deploy frontend/dist"
  },
  "dependencies": {
    "hono": "^4.9.0",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "5.20260916.1",
    "typescript": "^5.9.2",
    "wrangler": "4.132.0"
  },
  "engines": {
    "node": ">=22"
  }
}
