name = "rede-lua-educacao"
compatibility_date = "2026-09-15"
pages_build_output_dir = "frontend/dist"

# Configure no Cloudflare Pages:
# SUPABASE_URL / SUPABASE_PUBLISHABLE_KEY
# SUPABASE_SERVICE_ROLE_KEY (secret, só backend)
# RESEND_API_KEY (secret, opcional para envio do formulário)
# CONTACT_TO_EMAIL / CONTACT_FROM_EMAIL
# As VITE_* são variáveis de build do frontend.

# Avatar AI (backend)
# POLLINATIONS_BASE_URL / POLLINATIONS_MODEL / POLLINATIONS_EDIT_MODEL
# POLLINATIONS_TOKEN (secret, opcional)
